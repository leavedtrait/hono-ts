{
  "name": "hono-ts",
  "scripts": {
    "dev": "bun run --hot src/app.ts",
    "build":"bun build src/app.ts --compile --outfile app",
    "start": "bun install && bun run build && ./app"
  },
  "dependencies": {
    "hono": "^4.5.3",
    "hono-sessions": "^0.3.3",
    "oslo": "1.2.1"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}